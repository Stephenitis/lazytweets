<div class="container">
  <h1>Tweet Now! 2: Multi-User</h1>

  <%
    # In our real-world application, this is not what we want. Instead, we should
    # be checking to see if there is currently a user logged-in, and if there is,
    # we should show her a form that will allow her to send a tweet. If there is no
    # user logged-in, we should show the sign-in button.
    %>
    <% if @user %>
    <div>
      <p>
        <p><%=@user.username%></p>
        <strong>token:</strong> <%= @user.oauth_token %><br>
        <strong>secret:</strong> <%= @user.oauth_secret %><br>
      </p>
      <p>
        <button><a href="/sign_out">Sign Out</a></button>
      </p>
      <div id="tweet-box">
        <form id="tweet-form" method="POST" action="/tweetout">
          <input type="text" maxlength="140" name="text" placeholder="tweet something fool">
          <input type="hidden" name="user_id" value="<%=@user.id%>">
          <input type="submit" value ="Submit">
        </form>
      </div>
    </div>
    <% else %>
    <button id="button"><a href="/sign_in">Sign In with Twitter</a></button>
    <% end %>
    <img src="/cookie.gif">
    <div id="waiting">

    </div>
  </div>
</div>
